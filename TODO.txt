- remap keys that have default vim behavior 
    - remap <Leader> (currently , )
    - remap window moving (CTRL-K, CTRL-L)
    - remove <f1> mappings
- get familiar with surround
- learn to use easy motion
- add python support (and maybe Ruby)
- figure out why FuzzyFinder is so slow/stalling
- add emacs Ctrl-A, Ctrl-E, etc. mapping in command line (see Tim Pope's .vimrc?)
- try vim-multiple-cursors (https://github.com/terryma/vim-multiple-cursors)

#########FUTURE plugins to look into ##########
- SuperTab
- snipmate

" map Ctrl+Space as Escape key
"noremap <C-Space> <Esc> // currently doing some sort of paste if no edit is made


" map <space> to act like a web browser's page down
"nnoremap <space> <PageDown>
"nnoremap <S-space> <C-u> "TODO: figured out how to configure terminal to send proper code
"vnoremap <space> <PageDown>
"vnoremap <S-space> <C-u>


" auto complete brackets // TODO: find better alternative...these interfere when pasting code
"inoremap {      {}<Left>
"inoremap {<CR>  {<CR>}<Esc>O<Tab>
"inoremap {{     {
"inoremap {}     {}
"inoremap {%     {%%}<Left><Left>
"inoremap {{     {{}}<Left><Left>
"inoremap {}     {}
